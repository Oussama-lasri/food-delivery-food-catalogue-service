server:
  port: 9098

spring:
  application:
    name: FOOD-CATALOGUE-SERVICE
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
	
	
	